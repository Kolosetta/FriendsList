<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/circleProfilePhoto"
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:layout_margin="12dp"
        app:civ_border_width="1dp"
        app:civ_border_color="#000000"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:src="@drawable/female_1"
        />

    <TextView
        android:id="@+id/fullName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        tools:text="My text"
        android:textSize="20sp"
        app:layout_constraintStart_toEndOf="@+id/circleProfilePhoto"
        app:layout_constraintTop_toTopOf="@+id/circleProfilePhoto"
        app:layout_constraintBottom_toBottomOf="@+id/circleProfilePhoto"
        app:layout_constraintEnd_toEndOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>